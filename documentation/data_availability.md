# Data availability

The repository includes selected processed tables, metrics, audit records, candidate structures, DFT input templates, and the frozen Quantum ESPRESSO spot-check package.

The large raw Materials Project acquisition payload is not redistributed. Authorized users may regenerate it through the extraction workflow. The raw sodium JSON is classified as `runtime_generated_not_bundled`; substituting a processed CSV is not permitted.
