# Figure-generation policy

The `figures/` directory may remain empty in the clean-room snapshot. Publication figures must be regenerated from audited notebooks and selected result tables after repository-wide validation, linked to source hashes, and visually inspected before release.
